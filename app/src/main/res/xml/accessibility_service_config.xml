<?xml version="1.0" encoding="utf-8"?>
<!--
  NOTE: android:packageNames is deliberately absent.
  Filtering by package here would mean we never hear about leaving the target app,
  so the last screen's boxes would linger in the wrong place. We take every event
  and check rootInActiveWindow.packageName ourselves.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeViewScrolled"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:notificationTimeout="100"
    android:description="@string/service_description" />
